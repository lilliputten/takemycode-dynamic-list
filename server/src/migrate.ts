/* eslint-disable no-console */

export async function migrate() {
  // TODO: Implement migrations using `scheme_migrations` table and `src/migrations/` files
  console.log('Migrating has not implemented yet');
  // DEBUG: Emulate some async ops
  return new Promise((resolve) => {
    setTimeout(resolve, 3000);
  });
}
